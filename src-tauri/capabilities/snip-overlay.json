{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "snip-overlay",
  "description": "Capability for per-monitor region-selection overlay windows (snip-*)",
  "windows": ["snip-*"],
  "permissions": ["core:default", "core:window:allow-close"]
}
